{
  "name": "holla-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Holla safety-app backend: identity, phone-based circles, alert fan-out (FCM + inbox), server-side dead-man's switch.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.7.0"
  }
}
